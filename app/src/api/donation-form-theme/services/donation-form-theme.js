'use strict';

/**
 * donation-form-theme service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::donation-form-theme.donation-form-theme');
